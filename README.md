# todo-list
To run this project please follow this steps:
1. In main folder run the command: "npm install".
2. In client folder run the commands: "npm install" , "npm run build".
3. In main folder run the command: "npm start".

Then go to localhost:4000
